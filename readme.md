# FastAPI user management template app ready to be deployed to a kubernetes cluster
Local development files:
* `docker-compose.yml`
* `./docker/dev.Dockerfile`
---
Production / staging deployment files:
* `./docker/Dockerfile`
* `./k8s`
*  `./fastapi-template-chart`